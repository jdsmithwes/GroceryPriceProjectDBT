# Copy to .env and fill in real values. .env is gitignored — never commit it.

# Apify (used to run the Publix scraper actor)
APIFY_API_TOKEN=
# owner~actor-name, e.g. rigelbytes~publix-scraper or scraptivo~publix-scraper
APIFY_PUBLIX_ACTOR_ID=rigelbytes~publix-scraper
